# Agent Configuration
AGENT_TYPE=llm  # Options: llm, react, multi
OPENAI_API_KEY=your-api-key-here
MODEL=gpt-4o-mini

# Server Configuration
HOST=0.0.0.0
PORT=8000

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=*
